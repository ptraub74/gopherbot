{
	"PluginPath": "plugins/repeat.rb",
	"Channels": [ ],
	"Help": [
		{
			"Keywords": [ "repeat" ],
			"Helptext": [ "(bot), repeat <simple text> - trivially repeat a phrase" ]
		}
	],
	"comment": "Note that backslashes need to be escaped as double backslashes, e.g. \\",
	"CommandMatches": [
		{
			"Command": "echo",
			"Regex": "echo ([.;!\\d\\w-, ]+)"
		}
	]
}
